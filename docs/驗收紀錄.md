# 墨頁 PDF 0.15.4 驗收紀錄

驗收日期：2026-09-17
環境：Windows 11 x64（10.0.26200）、Intel64 Family 6 Model 151、實體記憶體 8.3 GB。

## 本版重點

- 修正蓋章與簽名圖層在高 DPI 螢幕上模糊：畫布圖層依裝置像素比例縮放並設定 `devicePixelRatio`，保留原始解析度影像，拖曳縮放時一律由原圖取樣，避免重複縮放。
- 手寫簽名改以 4 倍超取樣與浮點座標錄製，輸出 PNG 時裁除四周空白，放入頁面後筆跡清晰且填滿圖框。
- 新增 `test_canvas_layer_uses_device_pixels_on_high_dpi`、`test_signature_png_is_high_resolution_and_cropped` 回歸測試；全測套件 320 項通過，0 項失敗。

## Windows 封裝與安裝

- Python 3.12（`work\build-venv312`）、Inno Setup 6.4.3 命令列編譯器（`C:\Tools\Inno\ISCC.exe`），以 `scripts\build.ps1` 建置（含 `pytest -q` 全測通過）。
- PyInstaller 封裝成功（`dist\LocalPDFEditor`）；Inno Setup 回報 `Successful compile`，產生 `dist\installer\LocalPDFEditor-Setup.exe`（66,549,894 bytes，SHA256 `C00063985BC682A3B0EA03D82A6C4E2F2A48D5CAE4D0D041F7ABE7B2D487A1FA`）。
- 安裝程式靜默安裝至 `work\v0154-install`，結束代碼 0（註冊表 `DisplayVersion=0.15.4`）；從安裝目錄啟動 `--smoke-test` 開啟實際文件 `115.01.02-2014-F.pdf` 完成渲染與視窗擷取（`v0154-smoke.png`，248,260 bytes），程序結束代碼 0。

---

# 墨頁 PDF 0.15.3 驗收紀錄

驗收日期：2026-09-16
環境：Windows 11 x64（10.0.26200）、Intel64 Family 6 Model 151、實體記憶體 8.3 GB。

## 本版重點

- 修復子集化字型後文字渲染消失問題：`subset_font` 以 fontTools 子集化 CID-keyed CFF 字型（如 Noto Sans CJK）時，fontTools 預設會重排 glyph ID，破壞 CharStrings 索引與 FDSelect 對應，導致 PyMuPDF 輸出的子集字型無法光柵化（text layer 正常但渲染墨水為零）。修正方式：設定 `options.retain_gids = True` 保留原始字形編號，並修正字符篩選邏輯保留 U+0020（空格）與 U+00A0（NBSP），避免空白字元被剃除。
- 新增 `test_subset_font_keeps_regular_space_glyph`、`test_subset_font_keeps_nbsp_glyph` 回歸測試驗證子集字型保留空白字元；新增 `test_bold_replace_preserves_text_after_space` 驗證粗體後空白後文字保留。引擎層與端對端全測套件 318 項通過，0 項失敗。
- 像素驗證：run 6:0（r60）與 run 7:1（r71）的墨水差異均為 0，確認子集字型渲染與完整字型一致。

## Windows 封裝與安裝

- Python 3.12.10、PySide6 Essentials 6.8.3、PyMuPDF 1.28.2、PyInstaller 6.22.2、Inno Setup 6.4.3 命令列編譯器（`C:\Tools\Inno\ISCC.exe`）。
- `pytest -q`：318 項通過，0 項失敗（約 81 秒）。
- PyInstaller 封裝成功（`dist\LocalPDFEditor`）；Inno Setup 回報 `Successful compile`，產生 `dist\installer\LocalPDFEditor-Setup.exe`（65,240,770 bytes，SHA256 `EE76607526A311C3BF23F9646304A98561431871B99305E604FCC0226AF93ACA`）。
- 安裝程式靜默安裝至 `work\v0153-install`，結束代碼 0（註冊表 `DisplayVersion=0.15.3`）；從安裝目錄啟動 `--smoke-test` 開啟實際文件 `115.01.02-2014-F.pdf` 完成渲染與視窗擷取（`v0153-smoke.png`，248,260 bytes），程序正常結束且無殘留程序。

---

# 墨頁 PDF 0.15.2 驗收紀錄

驗收日期：2026-09-16
環境：Windows 11 x64（10.0.26200）、Intel64 Family 6 Model 151、實體記憶體 8.3 GB。

## 本版重點

- 文字編輯效能最佳化：`subset_font` 以 fontTools 將系統字型預先子集化（僅含本次編輯所需字元，輸出約 4–9 KB）後嵌入 PDF，子集成功時跳過整份文件的 `doc.subset_fonts` 全檔掃描；`find_table_cell` 表格偵測加 `lru_cache`。以互動基準對照，編輯引擎整體耗時由約 1466 ms 降至快取命中約 290–330 ms、換新文字約 560–690 ms；輸出檔約 447–455 KB，維持回歸測試的 500,000 bytes 上限門檻。
- 子集化失敗時保留原字型路徑並沿用原始 `doc.subset_fonts(fallback=True)` 流程，行為不變。
- 新增 `tests/test_fonts.py` 與 `tests/test_text.py` 子集/快取回歸測試；引擎層與端對端全測套件 315 項通過，0 項失敗。

## Windows 封裝與安裝

- Python 3.12.10、PySide6 Essentials 6.8.3、PyMuPDF 1.28.2、PyInstaller 6.22.2、Inno Setup 6.4.3 命令列編譯器（`C:\Tools\Inno\ISCC.exe`）。
- `pytest -q`：315 項通過，0 項失敗（約 61 秒）。
- PyInstaller 封裝成功（`dist\LocalPDFEditor`）；Inno Setup 回報 `Successful compile`，產生 `dist\installer\LocalPDFEditor-Setup.exe`（65,239,912 bytes，SHA256 `F29AE2B8356E9F43EB8BC9DCDAEC6424602CA94BD75B5C3129F3BBF5AE583F17`）。
- 安裝程式靜默安裝至 `work\v0152-install`，結束代碼 0（註冊表 `DisplayVersion=0.15.2`）；從安裝目錄啟動 `--smoke-test` 開啟實際文件 `115.01.02-2014-F.pdf` 完成渲染與視窗擷取（`v0152-smoke.png`，248,260 bytes），程序正常結束且無殘留程序。

---

# 墨頁 PDF 0.15.1 驗收紀錄

驗收日期：2026-09-16
環境：Windows 11 x64（10.0.26200）、Intel64 Family 6 Model 151、實體記憶體 8.3 GB。

## 本版重點

- 修復「勾選粗體後欄位內文字消失」：`_cell_from_drawings` 會把含圖像底匡的裝飾框（如標題橫幅）誤判為表格儲存格，勾選粗體時 commit 把文字置中重繪到整個框的中央，原本欄位位置墨點掉 92%（0.183 → 0.023）。現在排除被頁面圖像覆蓋超過 50% 的候選框（新增 `_image_covers_rect`），文字保持在原欄位並正常加粗（墨點 0.183 → 0.218）；新增 `test_find_table_cell_ignores_image_backed_frame` 回歸測試。
- 真實既有表單 PDF 表格值（8:1 等）粗體後文字保留、墨點增加（0.127 → 0.151），表格線完整，不受影響。
- 引擎層與端對端全測套件 315 項通過，0 項失敗。

## Windows 封裝與安裝

- Python 3.12.10、PySide6 Essentials 6.8.3、PyMuPDF 1.28.2、PyInstaller 6.22.2、Inno Setup 6 命令列編譯器。
- `pytest -q`：315 項通過，0 項失敗。
- PyInstaller 封裝成功（`dist\LocalPDFEditor`）；Inno Setup 回報 `Successful compile`，產生 `dist\installer\LocalPDFEditor-Setup.exe`（65,244,760 bytes，SHA256 `3fecae15963b10a1b0c56bad567ed20c19a10a6509176f6dab5f79ee59731716`）。
- 安裝程式靜默安裝至 `work\v0151-install`，結束代碼 0；從安裝目錄啟動 `--smoke-test` 開啟實際文件 `115.01.02-2014-F.pdf` 完成渲染與視窗擷取，結束代碼 0。

---

# 墨頁 PDF 0.15.0 驗收紀錄

驗收日期：2026-09-16
環境：Windows 11 x64（10.0.26200）、Intel64 Family 6 Model 151、實體記憶體 8.3 GB。

## 自動測試與核心流程

- Python 3.12.10、PySide6 Essentials 6.8.3、PyMuPDF 1.28.2。
- `pytest -v`：314 項通過，0 項失敗（約 3 分鐘）。
- 頁面底圖依 Windows 裝置像素比例直接渲染，避免二次放大或縮小；縮放尺寸及文字點選座標維持一致。
- 端到端測試涵蓋：選取文字、替換文字、加入圖章、另存、復原、重做、再次另存、拆分頁面及來源檔雜湊不變；另涵蓋將舊 PDF 的唯一藍色影像轉為圖章、另存、關閉、重開、移動、再次另存，以及像素比對確認原位置為白色且新位置保留藍色，沒有殘影或重影。
- 互動測試涵蓋：中央頁面內直接修改與新增文字、Enter 或點到別處立即寫入、Esc 取消、左鍵平移頁面、拖曳文字並立即套用、Delete 刪除後原位新增、可見的新增模式提示、三種文字對齊選項、表格儲存格偵測，以及窄儲存格的即時輸入框中心、文字對齊同步與完整字高顯示。
- 頁面管理測試涵蓋：`Ctrl` 不連續多選、`Shift` 連續範圍多選、單頁及多頁縮圖直接拖曳排序、拖放位置換算、群組相對順序保留、多頁批次上移／下移、多頁批次向左／向右旋轉、多頁批次複製、拖曳或數值裁切及刪除、最後一頁保護、縮圖與頁碼同步，以及操作後復原頁面狀態。
- 頁面新增與輸出測試涵蓋：依選取位置插入外部 PDF 全部頁面、沿用參考頁尺寸與旋轉方向新增空白頁、插入後自動選取、插入與空白頁復原，以及依選取順序抽取獨立 PDF 且不修改目前工作階段。
- 全文搜尋測試涵蓋：整份文件逐頁比對、回傳每筆頁碼與位置、空白關鍵字拒絕、介面結果計數、跨頁下一筆導覽及黃色標示；另驗證 `Ctrl+F`、`F3` 與 `Shift+F3` 快捷鍵設定。
- OCR 測試涵蓋：Tesseract TSV 格式與 Windows 5.5.2 無標頭輸出、35 信心門檻、300 DPI 座標轉換、繁中及英文內建模型驗證、原畫面不變的不可見文字層、已有至少 10 個文字字元自動跳過、多頁失敗不留部分變更，以及主視窗單次復原。
- 頁面比較測試涵蓋：144 DPI RGB 白底渲染、不同尺寸等比例置中、24 通道差異門檻、45% 紅色覆蓋、三欄預覽、不同頁碼切換、密碼 PDF、失效背景結果隔離，以及不改變 PDF、`revision` 與復原歷史。
- 縮放與圖片輸出測試涵蓋：適合頁面、適合寬度、25% 至 500% 逐級放大縮小、預設視窗寬度下按鈕直接可見、鍵盤快捷鍵、倍率邊界停用、縮放後維持原檢視中心、選取頁面依實際頁碼命名、144 DPI 像素尺寸、輸出包含目前圖層，以及目標檔已存在時停止且不覆寫。
- 直接裁切測試涵蓋：中央頁面綠框與八個控制點、右下角拖曳後的頁面座標換算、放開立即批次套用、進入模式時頁面選取範圍的保留、`Esc` 取消，以及切換至文字模式時自動結束裁切。
- 頁碼與浮水印測試涵蓋：非連續選取頁面的連續編號、六種頁碼位置檢查、內建中文字型、文字浮水印透明圖產生、圖片浮水印透明度及比例、無效設定拒絕，以及只修改選取頁面。
- 頁首頁尾範本測試涵蓋：六種位置、實際頁碼與總頁數欄位替換、只修改選取頁面、範本新增、同名覆寫、重新載入、刪除及空白內容拒絕。
- 標記註解測試涵蓋：選單所有功能在可編輯文件中均可點選、缺少文字選取時顯示操作提示、自動結束新增文字、文字註解、選取註解與裁切等衝突模式、下一次頁面點擊會選取文字而不會誤新增註解、無法替換內容的文字不會開啟行內編輯器但仍可寫入原生螢光標記或底線、在指定頁面座標加入原生文字註解、空白註解及未知標記型別的拒絕處理，以及介面操作後復原。
- 註解管理測試涵蓋：讀取頁面原生註解及位置、中央頁面點選、未選註解時由改色或刪除功能進入選取模式、Delete 刪除、黃／綠／粉紅／藍四色切換、非螢光註解的改色提示，以及改色後復原。
- 圖層縮放測試涵蓋：選取圖章後顯示四角控制點、四個控制點位於圖片內外側的完整白色區域皆可拖曳、放開後送出新位置與尺寸，以及縮放前後維持原始長寬比例。
- 可編輯圖章工作層測試涵蓋：保存乾淨基底、資產雜湊、頁面與幾何資料，關閉重開後還原可移動圖章，移動後再次另存不重複疊加，以及工作層遺失或資料毀損時安全回退為可見的靜態 PDF。
- 既有圖章轉換測試涵蓋：只列出全文件唯一使用且小於頁面 90% 的影像、候選縮圖與明確選取、背景掃描與轉換期間停用操作、失效結果隔離、轉換後自動選取、復原／重做，以及拒絕整頁掃描、跨頁重複 Logo、相同位置重複繪製、向量內容與無候選文件，拒絕時不修改 PDF。
- 另存採新檔名，不允許覆寫來源；既有表單、憑證簽章、附件及禁止修改的文件會停止高風險操作。
- 字型與粗體測試涵蓋：列舉 Windows 系統字型（本機 644 個）、字型選單首項為內建 Noto Sans CJK、具備真粗體檔的字型（JhengHei Bold）直接使用字型檔且內容流不含 `2 Tr`、輸入缺少字型支援的字元時警告顯示與隱藏、介面勾選粗體後套用 `2 Tr` 繪製、移動 run 後粗體保留、刪除 run 無異常，以及模擬粗體渲染品質檢查（實際文字 bbox 墨水覆蓋率低於 0.70，不成團狀黑塊）。

## Windows 封裝與安裝

- PyInstaller 6.22.2 目錄式封裝成功。
- 本次功能分支以 PowerShell 7、PyInstaller 6.22.2 及 Inno Setup 6.4.3 重新建置成功；建置腳本內含的 279 項測試全部通過，產生 `LocalPDFEditor-Setup.exe`，Inno Setup 回報 `Successful compile`。
- 已完成的 Qt/API 自動化端對端驗收（不是凍結安裝版的原生 UI 操作）：以含藍色影像的測試 PDF 執行舊章轉換、第一次另存、關閉工作階段、重開、移動及第二次另存。第二次另存檔重新開啟後有一個可編輯圖層；原位置中心像素為白色 `(255, 255, 255)`，新位置中心像素為藍色 `(0, 40, 255)`，且仍包含工作層描述與乾淨基底。
- 已完成的凍結安裝版 smoke：將目前分支重新建置的安裝程式靜默安裝至工作樹內隔離目錄，再以實際安裝的 `LocalPDFEditor.exe` 開啟含一個可編輯圖章及一筆原生註解的工作層 PDF，擷取 63,943 bytes 視窗影像；畫面正確顯示藍章、黃色註解與「可編輯」狀態，程序正常結束且殘留程序為 0。此項只驗證安裝版可重開工作層、正確渲染及正常退出。
- 未完成的凍結安裝版原生 UI 驗收：在安裝版介面中實際點選候選縮圖、按下轉換、用滑鼠拖曳、另存及重開。本次工作階段未提供原生應用程式控制介面，因此不得將 Qt/API 端對端測試視為這項手動／原生 UI 驗收已完成。
- smoke 完成後已靜默解除安裝，隔離安裝目錄已移除；建置輸出與可重複檢查的 smoke 輸出保留在工作樹忽略目錄。
- 凍結版開啟純影像測試 PDF、完成頁面渲染與視窗擷取，結束代碼 0，沒有殘留程序。
- PyInstaller 明確收入 Windows tesserocr wheel 需要的 `tesserocr.cysignals` 與 `tesserocr.cysignals.signals`；回歸測試會檢查 spec，防止封裝版運行時遺漏該 extension。
- Inno Setup 6.4.3 使用繁體中文介面編譯成功，無編譯警告。
- 0.15.0 以 PowerShell 7、PyInstaller 6.22.2 及 Inno Setup 6.4.3 重新建置成功；建置腳本內含的 314 項測試全部通過，產生 `LocalPDFEditor-Setup.exe`，Inno Setup 回報 `Successful compile`。模擬粗體渲染回歸測試（`test_simulated_bold_render_is_not_solid_blob`）確認原 `border_width=size*0.04` 造成的 88.70% 墨水覆蓋（團狀黑塊）修正為固定 `0.01` 後約 42.9%–44.1% 的字級穩定覆蓋，人眼不再出現「一團黑」。
- 0.14.3 安裝程式靜默安裝至工作樹內已核對的 `work/v143-final-install`，結束代碼 0；開啟實際文件 `115.05.26-6061-F-一昌.pdf` 完成渲染及視窗擷取，結束代碼 0。
- 從安裝目錄啟動私有 `--ocr-comparison-smoke-test`，以純影像 PDF 執行實際 OCR：213 個文字區段，搜尋「材料」命中 3 筆；輸出 13,950,977 bytes 可搜尋 PDF，以外部 Python 重新開啟後 `find_text()` 仍命中 3 筆。
- 同一安裝版驗收開啟頁面比較，顯示相似度 99.3% 與差異 13,041 / 2,003,960 像素，`revision` 為 0 → 0；已擷取 398,959 bytes 的搜尋與比較合併預覽。
- 驗收程式、兩個 worker 均自動結束，殘留程序為 0。靜默解除安裝成功，結束代碼 0，隔離安裝目錄已移除。

本次安裝驗收在建置電腦執行；尚未在另一台完全未安裝 Python 的 Windows 11 電腦實測。安裝包已包含 Python、Qt、MuPDF 及所需動態函式庫，不會依賴專案虛擬環境。

## PDF 相容性

- PyMuPDF 產生的編輯結果可重新開啟並擷取文字。
- 將實際文件 `115.01.02-2014-F.pdf` 首頁以 144 DPI 產生純影像單頁 PDF，OCR 前無可擷取文字；以實際 tesserocr 2.10.0、Tesseract 5.5.2 及 `chi_tra+eng` 模型辨識後寫入 213 個文字區段，可擷取「材料 測試 報告 / Meterial Test Report」等文字。
- 同一份 OCR PDF 以核心 `find_text()` 及主視窗搜尋「材料」均命中 3 筆，主視窗顯示黃色搜尋結果。
- 在純影像副本左上方 80 × 40 PDF 點的已知空白區加上黑塊後比較：差異 13,041 / 2,003,960 像素，相似度 99.3%，黑塊對應區有 12,800 個紅色像素；主視窗比較前後 `revision` 為 0 → 0，未改變。
- 使用實際文件 `115.01.02-2014-F.pdf` 驗證：同一行 354 個碎片文字區段可合併為 142 個可選取區段；「韋伯式(HW)」可在短列高儲存格內改為置中的「硬度」，且表格線保持完整。
- 使用實際文件 `115.05.26-6061-F-一昌.pdf` 驗證：將第 1-3 列「長度」改為任意三字文字並水平置中後，文字中心與儲存格中心誤差小於 0.001 PDF 點；即時輸入框不再因固定最小寬度而向右偏移。
- 實際主視窗流程驗證：文字拖曳後 X 座標確實向右移動；刪除後在中央頁面原位置直接輸入「硬度」，再用工具列新增「新增測試」，四次變更均進入可復原歷史。
- 以實際文件複製成三頁後驗證：頁面下移、順時針旋轉、刪除、復原與重做均成功；頁面操作前的圖章會先固定至 PDF，復原後可回到仍能個別調整的圖層狀態。
- 以同一份三頁文件驗證：將第一頁縮圖拖至最後一頁後，文件內容順序確實變為第 2、3、1 頁；在所選文字寫入螢光標記，並於頁面指定位置加入文字註解，重新開啟工作階段 PDF 後可讀到 `Highlight` 與 `Text` 兩種原生註解。
- 以實際三頁文件驗證：黃色螢光標記改為綠色後，重新讀取 PDF 的原生色彩值正確；文字註解可在中央頁面直接點選並按 Delete 移除，最後只保留改色後的螢光標記。
- 以實際文件驗證：頁面上的圖章可由四角控制點直接拖大，右側寬高同步更新且維持 2:1 原始比例；縮放後再執行頁面旋轉，圖章可正常固定至 PDF，復原後仍能回到可調整圖層。
- 以實際文件複製成三頁後驗證：可同時選取第一頁及第三頁，也可連續選取第二頁及第三頁；批次上移後實際內容順序變為第 3、1、2 頁，選取範圍同步移至新位置；批次旋轉只改變選取頁面；批次刪除後只保留未選頁面，復原可完整恢復。
- 以實際文件複製成三頁後驗證：不連續選取第一頁與第三頁並整組拖至末端，實際內容順序變為第 2、1、3 頁，選取範圍同步移至新位置；複製同一組後內容依序成為第 1、2、3、1、3 頁，新副本保持選取，復原可回到原三頁。
- 以實際文件複製成三頁後驗證：三頁同時套用左、右各 5 點及上、下各 8 點裁切，頁面尺寸正確縮減；頁尾中央依序寫入第 101 至 103 頁；三頁皆加入半透明旋轉中文浮水印，第一頁另加入半透明圖片浮水印，重新開啟工作階段 PDF 後文字與圖片資源均可讀取。
- 以實際文件複製成三頁後驗證：中央綠色裁切框可直接啟動，拖曳結果以左、右各 5 點及上、下各 8 點批次套用；三頁頁尾依序寫入「必榮實業 內部檢驗文件 1/3」至「3/3」，重新開啟工作階段 PDF 後均可讀取。
- 以實際文件驗證：新增空白頁後頁數由一頁增為兩頁，空白頁尺寸及方向與原頁一致；再插入同一份原版 PDF 後增為三頁，新頁內容可正常讀取。選取第一頁及第三頁抽取後，新檔包含兩頁完整材料報告，且目前工作階段版本號未因抽取操作改變。
- 以實際文件驗證：搜尋「材料測試報告」找到第 1 頁唯一結果並在中央頁面顯示黃色標示；「適合頁面」及「適合寬度」均可正常重繪，適合寬度的比例不小於適合頁面。
- 以實際文件驗證：第 1 頁以 144 DPI 匯出為 1190 × 1684 PNG，檔名保留第 001 頁，輸出前後目前工作階段版本號保持不變。
- 同一份文件連續新增不同中文字時會使用相符的獨立字型資源，實測「硬度」及「新增測試」皆可正確顯示與擷取。
- 內嵌子集字型在重新寫入前會檢查 Unicode 往返結果；不可靠時改用內建中文字型，避免顯示內容與文字層不一致。
- Poppler `pdftoppm` 以另一套 PDF 引擎渲染編輯後文件，結束代碼 0；渲染結果可見替換後文字「檢測合格 XYZ 789」，原有「品質檢驗 ABC 123」與「KEEP ME」仍存在。
- Poppler 對測試檔回報缺少 Symbol 顯示字型的提示，但輸出 PNG 正常，測試內容未使用 Symbol 字元。
- 尚未完成 Microsoft Edge 內建 PDF 閱讀器的自動化驗證；先前自動化工具無法可靠確認瀏覽器目前 URL，因此未把該次操作列為通過。

## 效能量測

量測方法：單程序引擎測量；頁面以 125% 縮放及本機 1.25 裝置像素比例渲染；主程序及子程序 RSS 每 50 毫秒採樣，不含其他應用程式。數字為本機單次候選版結果。

| 頁數 | 開啟 | 首頁渲染 | 編輯 | 儲存 | 峰值 RSS |
|---:|---:|---:|---:|---:|---:|
| 1 | 0.0003 秒 | 0.0363 秒 | 0.8329 秒 | 0.0049 秒 | 193.3 MiB |
| 20 | 0.0003 秒 | 0.0216 秒 | 1.5951 秒 | 0.0247 秒 | 211.5 MiB |
| 100 | 0.0005 秒 | 0.0238 秒 | 3.1989 秒 | 0.0200 秒 | 207.9 MiB |

效能腳本與正式 Windows 封裝均使用 Python 3.12.10。PySide6 固定為 6.8.3，以避開 6.11.2 與 PyInstaller 在本機的 DLL 載入相容性問題。

## 已知限制

- 文字修改適用於可解碼、局部、橫排文字；不提供直排重排、斜向文字重排或全文自動排版。
- OCR 結果受解析度、壓縮、污點、旋轉、表格線與版面影響；只建立不可見文字層，不重建版面或代替人工校對。
- 頁面比較是像素層比對，字型渲染、掃描噪點、幾何位移、旋轉或頁面尺寸差異都可能被標示；相似度不是內容語意比較。
- 圖章與簽名是寫入頁面的外觀，不是憑證數位簽章；可編輯工作層只由「墨頁 PDF」維護，第三方壓縮、最佳化、列印成 PDF 或移除附件可能將其刪除，之後只能以靜態頁面內容開啟。
- 舊圖章轉換只支援全文件中唯一使用、單一位置且小於頁面 90% 的點陣影像；整頁掃描、重複 Logo／影像與向量章不會列為候選。
- 合併與拆分不重建書籤及文件內部連結。
- 已加密文件另存時不保留加密；程式會先提示。
- 此版本聚焦文字修改、蓋章、手寫或圖片簽名、合併及拆分，未宣稱完整重現 Adobe Acrobat 的所有功能。

OCR 封裝元件的授權為 tesserocr 2.10.0（MIT）、cysignals 1.12.6（LGPL-3.0-or-later）、Tesseract 5.5.2 與 tessdata_fast（Apache-2.0）及 Leptonica 1.87.0（BSD-2-Clause）；完整文字與來源收錄於 `licenses/`。
